# ImagePet V0.2 手动验收测试指南

本指南详细说明了如何对 ImagePet V0.2 版本进行端到端（E2E）手动功能与交互验收。测试基于 App Sandbox 启用的 macOS 13+ 本地运行环境。

---

## 准备工作

### 1. 编译并运行 App
在终端项目根目录下执行编译并启动脚本：
```bash
./script/build_and_run.sh
```
或者，如果您想观察 App 输出的日志，可以运行：
```bash
./script/build_and_run.sh --logs
```

### 2. 准备测试图片素材
准备一个包含以下类型测试图片的临时文件夹（建议放在桌面）：
- **常规图（5~10张）**：包含 `HEIC`、`PNG`、`JPG/JPEG` 格式。
- **坏图/坏格式（2张）**：一张损坏无法打开的 PNG 或是重命名后缀的假图，以及一张不支持的文件格式（如 `GIF`、`WebP` 或 `PDF`）。

---

## 验收测试用例

### 场景一：首次启动与输出目录授权 (Sandbox 校验)
**目的**：验证首次启动时是否能正确拦截并引导用户配置合规的安全书签目录。

1. **测试步骤**：
   - 如果是首次运行（或者通过命令行重置了 Defaults），App 启动后应立即自动弹出 macOS 的文件选择器（`NSOpenPanel`），要求选择输出目录。
   - 在选择器中，点击左下角“新建文件夹”，新建一个名为 `ImagePet Output` 的文件夹，然后点击“**Choose Folder**”。
2. **期望结果**：
   - 目录授权成功，App 主界面的底部控制栏中会显示：`Output Folder: /path/to/ImagePet Output`。
   - 界面没有任何黄色警告标志。

---

### 场景二：批量图片压缩核心流程 (Balanced 预设)
**目的**：验证基本拖入、并发排队、即时状态更新及体积节省汇总。

1. **测试步骤**：
   - 保持 Quality 预设为默认的 **Balanced**。
   - 全选准备好的常规图片，拖入 App 主窗口的虚线拖拽区域中；或通过点击“**Add Images**”按钮从文件管理器中全选并导入。
2. **期望结果**：
   - 列表立即列出所有图片，初始状态显示为 `Pending`。
   - 任务以最多 **2 个并发**（`maxConcurrentJobs = 2`）开始执行，执行中的任务显示为 `Processing...`，完成显示为 `Done`。
   - 每张图压缩完成后，右侧立即刷新压缩详情（例如：`3.4 MB -> 820 KB / -75.8%`）。
   - 主窗口左上角的宠物表情在处理中变为 **😋 (nom nom nom...)**，全部完成后变为 **🥳 (Done!)**。
   - 底部汇总栏显示本次批量的总体统计：
     - **Ate**（输入总大小）
     - **Pooped**（输出总大小）
     - **Saved**（保存总体积与比例）
   - 点击“**Reveal in Finder**”按钮，Finder 能够正确打开并高亮选择输出文件夹，其中应该生成了对应的后缀为 `_compressed.jpg` 的图片。

---

### 场景三：坏图拦截与局部失败重试流程
**目的**：验证批处理的鲁棒性，单个文件损坏或不支持不中断整体批次，并可针对失败进行重试。

1. **测试步骤**：
   - 点击主界面底部的“**Clear List**”清除上一次任务。
   - 拖入 3 张图片：1 张正常的 PNG，1 张损坏的 PNG，以及 1 张 GIF。
2. **期望结果**：
   - 正常图顺利通过并完成。
   - 不支持的 GIF 格式立即标红显示：`Failed: Unsupported image format`。
   - 坏图解码失败标红显示：`Failed: Failed to decode image`。
   - 主窗口左上角宠物表情变为 **😵 (Done with issues)**，显示“`1 succeeded, 2 failed`”。
   - 底部控制栏右侧应显示“**Retry Failed**”按钮，点击它，App 将仅重新对那 2 张失败的图发起压缩流程（再次验证失败原因）。

---

### 场景四：桌面宠物悬浮小窗交互
**目的**：验证 Desktop Pet 窗口跨 Space 浮动、自由拖动、关闭、以及表情实时状态联动。

1. **测试步骤**：
   - 点击主界面上的“**Show Pet**”按钮（或使用快捷键 `Cmd + Shift + P`）。
   - 验证桌面右下角是否出现一个小型的宠物悬浮方块。
   - 按住悬浮窗的背景并尝试在屏幕上自由拖拽。
   - 尝试在 Mac 的不同 Spaces（虚拟桌面）之间切换，验证该悬浮窗是否依旧浮动显示在最前（`.floating` 层级）。
   - 再次尝试拖入几张大图进行压缩，观察悬浮窗宠物的表情是否从 **🐡 (Ready)** 切换到 **😋 (Eating)**，最后变成 **🥳 (Done)**。
   - 点击悬浮小窗右上角的圆形叉号“**x**”，或者点击主窗口的“**Hide Pet**”按钮。
2. **期望结果**：
   - 悬浮小窗能够平滑关闭。主窗口的按钮状态自适应变更为“**Show Pet**”。

---

### 场景五：权限失效与防覆盖保护
**目的**：验证安全目录失效防护，以及重名文件写入分配。

1. **测试步骤**：
   - **防覆盖测试**：
     - 不要清空输出目录，再次拖入同名的一张图片进行压缩。
     - 观察输出文件夹内产生的文件名。
     - *期望结果*：不覆盖已有文件，在输出文件夹中生成 `filename-ext_compressed-2.jpg`，以此类推。
   - **权限失效测试**：
     - 关闭 App。
     - 在 Finder 中找到此前选择的输出文件夹 `ImagePet Output`，并将其**重命名**为 `ImagePet Output Changed`。
     - 重新启动 App。
     - *期望结果*：由于书签指向的原始目录被修改或移动，App 会检测到 Sandbox 权限失效，在主界面上提示重新选择文件夹，或者给出黄色警告：`Output folder unavailable`。重新点击 `Choose Folder` 并指派新的目录后可以正常工作。

---

---

### 场景六：双态切换与折叠交互 (Mini <-> Full)
**目的**：验证桌面 Pet Mini 和 Full 模式之间能够顺畅切换，且缩放时窗口中心点保持锚定，无位移跳跃。

1. **测试步骤**：
   - 确保输出目录已配置（即非阻塞状态）。点击主界面上的“**Show Pet**”按钮（或使用快捷键 `Cmd + Shift + P`）。
   - *期望结果*：桌面上出现一个极其小巧的 Mini Pet（大小为 80x80，透明底，仅显示宠物表情，没有任何多余按钮或文字）。
   - 点击 Mini Pet 身体（表情）。
   - *期望结果*：Pet 顺利展开为 Full 状态（大小为 192x176，显示毛玻璃材质背景，以及 Ready、详情文字和 Add 按钮等），且**展开过程中窗口中心点不变，没有任何位置跳跃**。
   - 点击 Full 状态右上角的“**折叠**”按钮（即 chevron 图标）。
   - *期望结果*：窗口顺利收回为 Mini Pet 状态，且折叠过程中中心点锚定，不产生任何跳跃性位移。

---

### 场景七：右键 ContextMenu 与跳转
**目的**：验证 Mini / Full 状态下的右键控制菜单均能正常调出并工作。

1. **测试步骤**：
   - 在 Mini Pet 状态下右键点击宠物身体。
   - 验证菜单项：
     - 点击 **Show Panel** -> 期望变为 Full 状态。
     - 点击 **Open App** -> 期望唤醒并激活主窗口。
     - 点击 **Hide Pet** -> 期望关闭整个 Pet。
   - 重新开启 Pet 并展开为 Full，在 Full 面板上右键点击。
   - 验证菜单项：
     - 点击 **Collapse to Mini** -> 期望变回 Mini 状态。
     - 其它菜单项同上。

---

### 场景八：阻塞状态自动展开 (Force Full)
**目的**：验证在 output 目录缺失、覆盖确认或权限异常等阻塞流程时，Pet 能自动向用户解释。

1. **测试步骤**：
   - **NeedsSetup 测试**：
     - 关闭主窗口，移除或清空 UserDefaults 以复位书签，或从 App 主窗口将输出文件夹移除使主界面提示“Needs folder”。
     - 此时如果 Pet 是 Mini 状态，验证它是否**自动展开为 Full 状态**并展示“Needs folder”。
   - **Confirm 覆盖测试**：
     - 切换到 Overwrite 模式，并在 Mini 状态拖入一张同名图。
     - 验证 Pet 是否**自动展开为 Full 状态**，并在主窗口唤起覆盖确认 Sheet。

---

### 场景九：无交互自动折叠 (Idle Auto-Collapse) 与悬浮保护
**目的**：验证 Full 状态在 30 秒无交互后自动折叠回 Mini，且悬浮 Hover 能重置倒计时。

1. **测试步骤**：
   - 将 Pet 展开为 Full 状态，并且保持队列为空、无任何处理中任务。
   - 将鼠标移开小挂件区域，静置等待 30 秒。
   - *期望结果*：30 秒后，Full 面板自动收拢为 Mini 状态。
   - 再次展开为 Full，这次在等待过程中多次将鼠标悬停（Hover）在 Full 面板上方，或在面板上移动鼠标。
   - *期望结果*：每次悬停或移动都会重置 30 秒计时器，面板保持 Full 状态，不会在用户眼前突兀缩回。

---

## 验收结果检查清单 (Checklist)

| 测试项 | 测试场景 | 验收要点 | 是否通过 | 备注 |
| :--- | :--- | :--- | :---: | :--- |
| 1 | 首次启动 | 弹出输出目录 NSOpenPanel，授权后显示正确路径 | [x] | |
| 2 | 压缩格式 | HEIC/PNG/JPG 能全部被接收并输出标准 JPG | [x] | |
| 3 | 队列并发 | 队列最大并发限制为 2，UI 即时更新 | [x] | |
| 4 | 结果汇总 | 体积计算与节省率正确显示在 SummaryView | [x] | |
| 5 | 输出定位 | 点击 Reveal in Finder 正确打开 Finder 定位 | [x] | |
| 6 | 异常防御 | 不支持格式/坏图会报错，但不中断其他任务 | [x] | |
| 7 | 重试逻辑 | 点击 Retry Failed 仅重跑失败任务 | [x] | |
| 8 | 防重名 | 磁盘存在重名文件时，自动重分配为 `-2`, `-3` 后缀 | [x] | |
| 9 | 桌面小鸟 | 悬浮小窗支持跨 Space 浮动、拖动、以及表情同步 | [x] | |
| 10| 权限恢复 | 重命名输出文件夹后，能检测到权限失效并警告 | [x] | |
| 11| 双态切换 | Mini 点击展开为 Full，Full 点击折叠返回 Mini，且位置无位移 | [ ] | |
| 12| 右键菜单 | Mini/Full 右键均有控制菜单项，支持切换模式与唤起 App | [ ] | |
| 13| 阻塞展开 | needsSetup/confirm/permission 等阻塞状态自动从 Mini 展开为 Full | [ ] | |
| 14| 自动收回 | Full 状态无交互 30 秒自动收回 Mini，Hover 悬停能重置计时器 | [ ] | |
